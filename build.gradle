plugins {
    id 'java'
    id 'maven-publish'
}

group 'de.daver.buun'
version '1.0.1-alpha'

publishing{
    publications{
        mavenJava(MavenPublication){
            groupId = 'de.daver.buun'
            artifactId = 'core'
            version = '1.0.1-alpha'
            from components.java
        }
    }
}
